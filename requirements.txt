# The CGEL tree library, the JSON-LD converter, the UD->CGEL converter and the
# tree validator all live in this package now; installing it also pulls in
# jsonschema, pylatexenc, conllu, depedit and stanza.
-e ./packages/cgel-json-converter

tqdm
levenshtein>=0.20.9
pandas>=2.2.2
tabulate>=0.9.0
udapi>=0.3.0
nltk
pyyaml

# used only by the scripts under all-examples/, convertor/ and figures/
beautifulsoup4
lxml
mammoth
more-itertools
python-docx
pypdf
flask
plotnine
